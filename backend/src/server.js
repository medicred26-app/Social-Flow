console.log('SocialFlow publishing and auth now run in the Next.js app (port 4000).');
console.log('Start the app with: npm --prefix frontend run dev');
console.log('Due posts are processed by the Next.js scheduler when ENABLE_INLINE_SCHEDULER=true.');
process.exit(0);
